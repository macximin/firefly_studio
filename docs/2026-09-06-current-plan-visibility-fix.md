# 현재 기획과 이전 참고 표시 정정

2026-09-06

사용자가 제외한 시계 사건이 다시 보인다고 지적했다. 실제 금융형 v01의 이야기와 도입 1~4화에는 시계 사건이 없다. 직전 화면 변경에서 이전 9절 기획서를 현재 변주보다 먼저 펼친 것이 원인이었다. 양식 유지 요청을 과거 내용의 우선 노출로 잘못 구현했다.

현재 금융형 v01은 한수경 선점 → 파운드 옵션 → 회수 → 영국 제조사 지분 인수로 이어진다. 시계·연회 관련 표현은 수정 전 기준, 사용하지 않는 경험, 실제 원문 근거, 독립 심사의 변경 대조에만 남아 있다. 이번 수정에서 후보를 다시 쓰거나 모델을 호출하지 않았다.

## 표시 변경

- 현재 초반 수정안을 먼저 표시한다.
- 이전 9절·육하원칙은 아래의 닫힌 ‘이전 기준안 · 수정 전 참고’로 보존한다. 제외·교체한 사건도 포함된 과거 문서라고 명시한다.
- 변주 문서의 사건 대조와 실제 원문 근거도 닫힌 ‘이전·원문 비교 참고’로 표시한다.
- 현재 내용으로 통합한 전체 기획서는 아직 작성 전이라고 표시한다. 이전 연대·인물·장기 전개가 현재안으로 확정되었다고 표시하지 않는다.
- 각 절의 관련 수정안 링크, 정확한 기준 문서 연결, 현재 후보 판정은 그대로 유지한다.

## 검증

실제 v01·v02를 렌더링해 현재안 우선 순서, 닫힌 이전 문서, 참고 섹션, 유효한 앵커와 미선택 판정을 확인했다. v01의 기본 펼침 본문에는 한수경·풋옵션이 나오고 시계·연회는 나오지 않는다. 전체 렌더링에는 과거 자료가 보존된다. 후보·패킷 내용은 렌더링 전후 동일하다.

Storyyard 빌드 성공, 테스트 71개 통과·실패 0·선택적 v3 생산자 fixture 검사 1개 건너뜀. 독립 diff 검토에서 중대한 회귀를 찾지 못했다. 배포 전 소스 대비 추적된 data 및 호스팅 파일 27개가 바이트 동일하다. 원래 dirty 작업 사본은 수정하지 않았다.

게시 소스: `9d836644810ebddb8a6d7962a1ada6db534393b8`.

증거: `.firefly/current-plan-visibility-20260906/`의 `storyyard-test.log`, `preservation.json`, `archive-verification.json`, `deployment-receipt.json`. 브라우저 조작 검사는 수행하지 않았다. 게시 성공과 HTTP 응답은 영수증으로 별도 확인한다.

[현재 변주 검토](https://storyyard-wjjo.macximin11123.chatgpt.site/review/frp-30945a8618f92b17b5069c1c/v01)
